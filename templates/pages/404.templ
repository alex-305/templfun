package pages
import (
    "github.com/alex-305/templfun/templates/components"
)

templ NotFound() {
    <h4>@components.BackHome()</h4>
    <h2>We couldn't find the resource you were looking for</h2>
}